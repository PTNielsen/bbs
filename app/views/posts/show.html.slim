h1= @post.title

|  Authored by #{@post.author.email}

h4= @post.body

br/

- if can? :update, @post
  = link_to "Edit Post", edit_board_post_path(@board, @post), class: "btn btn-default"

br/
br/

- if can? :destroy, @post
  = link_to "Delete Post", board_post_path(@board, @post), method: :delete, class: "btn btn-default", data: {confirm: "Are you sure?"}

br/ 
br/

= link_to "Add Comment", new_board_post_comment_path(params[:board_id], params[:id]), class: "btn btn-default"

br/
br/

p Comments:

ul
- @comments.each do |comment|
  li= comment.body
  |  - #{comment.author.email}
  br/
  br/

br/